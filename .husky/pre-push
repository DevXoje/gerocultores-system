#!/bin/sh
# pre-push: run full test suite (frontend + API) before pushing
# Falls if any test fails — prevents broken code from reaching remote.
npm --prefix code/frontend run test && npm --prefix code/api run test
